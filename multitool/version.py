"""The app's own version.

Kept manually in sync with `MyAppVersion` in installer/Multitool.iss for
each release. multitool.updater compares this against GitHub's latest
release tag to decide whether an update is available.
"""

APP_VERSION = "0.1.0-beta"
